[project]
name = "presence"
version = "0.1.0"
description = "Experimental AI cognitive-exoskeleton and enhancer project"
readme = "README.md"
authors = [
    { name = "Ethan Caldwell", email = "332040004+eCaDDA@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
presence = "presence:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
